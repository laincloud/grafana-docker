sudo: true
language: python
python:
  - "3.6"
services:
  - docker
before_script:
  docker login --username $USERNAME --password $PASSWORD
script:
  - ./build.sh v4.4.4
  - ./push_to_docker_hub.sh v4.4.4
